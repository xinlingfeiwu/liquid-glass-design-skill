interface:
  display_name: "Liquid Glass Design"
  short_description: "Premium Liquid Glass UI workflows"
  default_prompt: "Use $liquid-glass-design to create, upgrade, and verify a premium Liquid Glass interface with SVG refraction, strong composition, motion, and accessible fallbacks."

policy:
  allow_implicit_invocation: true
